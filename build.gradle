wrapper.gradleVersion = '2.2.1'

allprojects {
    apply plugin: 'eclipse'
}

subprojects {
    apply plugin: 'java'

    group = 'org.ethereum'
    version = '0.7.14-SNAPSHOT'

    repositories {
        jcenter()
    }
}
